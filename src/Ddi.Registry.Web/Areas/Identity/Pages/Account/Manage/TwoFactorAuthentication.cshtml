@page
@model TwoFactorAuthenticationModel
@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer
@{
    ViewData["Title"] = Localizer["Title"].Value;
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h4>@ViewData["Title"]</h4>
@if (Model.Is2faEnabled)
{
    if (Model.RecoveryCodesLeft == 0)
    {
        <div class="alert alert-danger">
            <strong>@Localizer["NoRecoveryCodes"]</strong>
            <p>@Localizer["MustGenerateNewSet"] <a asp-page="./GenerateRecoveryCodes">@Localizer["GenerateNewSet"]</a> @Localizer["BeforeLogIn"]</p>
        </div>
    }
    else if (Model.RecoveryCodesLeft == 1)
    {
        <div class="alert alert-danger">
            <strong>@Localizer["OneRecoveryCode"]</strong>
            <p>@Localizer["CanGenerateNewSet"] <a asp-page="./GenerateRecoveryCodes">@Localizer["GenerateNewSet"]</a>.</p>
        </div>
    }
    else if (Model.RecoveryCodesLeft <= 3)
    {
        <div class="alert alert-warning">
            <strong>@Localizer["RecoveryCodesLeft", Model.RecoveryCodesLeft]</strong>
            <p>@Localizer["ShouldGenerateNewSet"] <a asp-page="./GenerateRecoveryCodes">@Localizer["GenerateNewSet"]</a>.</p>
        </div>
    }

    if (Model.IsMachineRemembered)
    {
        <form method="post" style="display: inline-block">
            <button type="submit" class="btn btn-primary">@Localizer["ForgetBrowser"]</button>
        </form>
    }
    <a asp-page="./Disable2fa" class="btn btn-primary">@Localizer["Disable2fa"]</a>
    <a asp-page="./GenerateRecoveryCodes" class="btn btn-primary">@Localizer["ResetRecoveryCodes"]</a>
}

<h5>@Localizer["AuthenticatorApp"]</h5>
@if (!Model.HasAuthenticator)
{
    <a id="enable-authenticator" asp-page="./EnableAuthenticator" class="btn btn-primary">@Localizer["AddAuthenticator"]</a>
}
else
{
    <a id="enable-authenticator" asp-page="./EnableAuthenticator" class="btn btn-primary">@Localizer["SetupAuthenticator"]</a>
    <a id="reset-authenticator" asp-page="./ResetAuthenticator" class="btn btn-primary">@Localizer["ResetAuthenticatorApp"]</a>
}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}